#include<stdio.h>
int main()
{
    printf("the size of the short int = %ld",sizeof(short));
    printf("the size of the int datatype = %ld\n",sizeof(int));
    printf("the size of the long int datatype = %ld\n",sizeof(long int));
    printf("the size of the float datatype = %ld\n",sizeof(float));
    printf("the size of the double datatype = %ld\n",sizeof(double));
    printf("the size of the long double datatype = %ld\n",sizeof(long double));
    printf("the size of the char datatype = %ld\n",sizeof(char));
}